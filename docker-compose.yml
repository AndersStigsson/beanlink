version: "3"
services:
  frontend:
    build:
      context: ./
      dockerfile: frontend.Dockerfile
    ports:
      - "10034:10034"
  backend:
    build:
      context: ./
      dockerfile: backend.Dockerfile
    ports:
      - "10033:10033"

